{
  "config": {
    "step": {
      "user": {
        "title": "Set up HassGlass",
        "description": "Choose defaults for all paired glasses. Devices pair themselves once the hub is set up — just put on the glasses and follow the on-HUD prompt.",
        "data": {
          "pipeline_id": "Default Assist pipeline ID (blank = use HA preferred)",
          "wake_word_enabled": "Enable on-glass wake word by default",
          "default_ttl_ms": "Default HUD card lifetime (milliseconds)",
          "fallback_media_player": "Fallback media_player entity ID for announcements"
        }
      },
      "confirm": {
        "title": "Pair {name}",
        "description": "A new HassGlass device is asking to pair. Enter the 6-digit code shown on its HUD to confirm.",
        "data": {
          "code": "Pairing code"
        }
      }
    },
    "abort": {
      "already_configured": "HassGlass is already set up. Only one hub per Home Assistant installation is supported.",
      "device_paired": "{name} is paired and will appear under the HassGlass hub.",
      "hub_not_configured": "Finish the HassGlass hub setup first, then put the glasses back on to pair them.",
      "invalid_discovery_info": "Discovered device did not advertise enough information to pair."
    },
    "error": {
      "invalid_code": "That code didn't match. Check the HUD and try again."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HassGlass defaults",
        "data": {
          "pipeline_id": "Default Assist pipeline ID",
          "wake_word_enabled": "Enable on-glass wake word by default",
          "default_ttl_ms": "Default HUD card lifetime (milliseconds)",
          "fallback_media_player": "Fallback media_player entity ID"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "battery": { "name": "Battery" },
      "signal": { "name": "Wi-Fi signal" },
      "last_intent": { "name": "Last intent" },
      "current_card": { "name": "Current HUD card" }
    },
    "binary_sensor": {
      "worn": { "name": "Worn" },
      "listening": { "name": "Listening" },
      "connected": { "name": "Connected" }
    },
    "button": {
      "dismiss_top_card": { "name": "Dismiss top HUD card" },
      "identify": { "name": "Identify" }
    },
    "select": {
      "pipeline": { "name": "Assist pipeline" }
    },
    "switch": {
      "wake_word": { "name": "On-glass wake word" },
      "listening_enabled": { "name": "Microphone enabled" }
    },
    "media_player": {
      "media_player": { "name": "Glasses speaker" }
    },
    "event": {
      "gesture": { "name": "Touchpad gesture" },
      "button": { "name": "Side button" }
    },
    "notify": {
      "notify": { "name": "HUD notification" }
    }
  },
  "services": {
    "notify": {
      "name": "Push HUD card",
      "description": "Show a card on a glasses device's HUD."
    },
    "dismiss": {
      "name": "Dismiss HUD card",
      "description": "Remove a HUD card by id, or all cards with card_id: \"*\"."
    },
    "identify": {
      "name": "Identify glasses",
      "description": "Briefly flash a checkmark on the HUD so the wearer knows which device was targeted."
    }
  }
}
